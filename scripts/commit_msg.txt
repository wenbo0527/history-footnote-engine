v2.10.1 W73: 修复 5 个 modal 不可用（恢复静态 import）

问题：玩家点 GameHeader 工具栏的 档案/回顾/词条/反馈/设置
按钮没反应。

根因（routes/game/+page.svelte）：
- W66 dynamic import 改成：
  let CharacterWikiModal: typeof X | null = $state(null);
- 模板中 <CharacterWikiModal /> 引用的是局部 null 变量
  （不是 import 的组件）→ 渲染 null
- loadModal() 函数定义了但 wikiOpen 变化时从未调用
- 5 个 modal 全部不可用

修复：
- 恢复静态 import：
  import { CharacterWikiModal, RecapModal, ... } from '...'
- 删除动态 import 逻辑
- 模板 <CharacterWikiModal /> 现在引用 import 的真实组件
- modal 立即可用

权衡：5 个 modal 总 size < 30KB，静态 import 影响极小
（路由级 code-split 仍生效，modal 不会阻塞首屏）

测试：173 PASSED 0 回归
